export const STATE_KEY = 'huebar.workspaceColors';

export const TITLE_BAR_KEYS = [
  'titleBar.activeBackground',
  'titleBar.inactiveBackground',
  'titleBar.activeForeground',
  'titleBar.inactiveForeground',
] as const;

export const STATUS_BAR_KEYS = ['statusBar.background', 'statusBar.foreground'] as const;
